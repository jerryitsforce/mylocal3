define([
  "jquery",
  'domReady!'
],function($) {
  return function (params, elem) {
    // $(elem).on('click', function() {
    //   $(this).parents('td.col.actions').find('.action.towishlist').trigger('click');
    // });
  };
});
